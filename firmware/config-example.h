#pragma once

// WiFi — reemplaza con tus credenciales reales
#define WIFI_SSID     "nombre-de-tu-red"
#define WIFI_PASSWORD "tu-contrasena"

// URL del túnel Cloudflare — se genera cada vez que inicias cloudflared
// Ejemplo: https://xxxx-xxxx-xxxx.trycloudflare.com
// Copia la URL real y reemplázala aquí, luego re-sube el firmware
#define SERVER_URL    "https://xxxx-xxxx-xxxx.trycloudflare.com"
